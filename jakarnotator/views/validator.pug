extends layout

block head
  meta(charset='UTF-8')
  meta(name='viewport', content='width=device-width, initial-scale=1.0')
  meta(http-equiv='X-UA-Compatible', content='ie=edge')
  title Document

  link(rel='stylesheet', href='/stylesheets/style.css')

  style.
    body {
      background-color:rgba(128, 128, 128, 0.9);
    }

block content
  .container-fluid
    .row.mb-5.mt-2
      .col-12.text-center
        h1 Validator tool page

    .row.mb-2
      .col-10.offset-1.col-lg-6.offset-lg-3.text-center
        p Does this polygon fit a
          b.text-primary  car
          |  ?

        .position-relative
          img(src="/images/validator_tool_sample.png", alt="validator tool sample", sizes="100vw").img-fluid
          .position-absolute(style="top:0;height:100%;width:100%;")
            .container(style="height:100%;")
              .row(style='background:transparent; height:100%;display:flex;align-items:center;')
                .col-12.text-center
                  p(style="transform: rotate(-35deg);font-size: 4vw;color:white") Coming soon !

    .row
      .col-2.col-lg-1.offset-lg-3
        button(type="button", title="Swap to left or press the left arrow").btn.btn-block.btn-danger 
          span(style="white-space:normal;") No :(
      .col-8.col-lg-4.text-center
        p Current score: +7 (based on 10 votes)
      .col-2.col-lg-1.text-right
        button.btn.btn-block.btn-success(type="button", title="Swap to right or press the right arrow")
          span(style="white-space:normal;") Yes :)
